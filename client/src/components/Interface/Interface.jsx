import React from 'react';

import {
  InterfaceContainer
} from './InterfaceStyles';

const Interface = ({ children }) => (
  <InterfaceContainer>
    {children}
  </InterfaceContainer>
)

export default Interface;